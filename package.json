{
  "name": "worldwind-earth-explorer",
  "version": "2.0.0",
  "private": true,
  "homepage": "https://worldwind.earth/explorer",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d root"
  }
}
